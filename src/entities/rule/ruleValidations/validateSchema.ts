import { InputValue, Validate } from "../ruleValidation";

/*
*  this function will validate all fields of the schema
*  @param {object} schema - the schema to be validated
*  @returns {boolean} - returns a boolean value
*/

export const validateSchema: Validate = (schema: InputValue): boolean => {
    return true;
}